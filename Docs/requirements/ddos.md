# DDoS 공격 탐지 요구사항

대상 코드: `src/jobs/DDos.job/job.ts`

## 책임 범위

- 동일 IP에서 특정 도메인으로 몰리는 대량 요청을 탐지한다.
- 동일 `client_ip`와 `domain` 조합을 기준으로 요청 수를 집계한다.
- 설정된 예외 IP는 탐지 대상에서 제외한다.

## 요구사항

| 이슈 번호 | 요구사항 ID | 구분 | 요구사항명 | 요구사항 설명 | 우선순위 | 난이도 | 비고 |
| ---: | --- | --- | --- | --- | --- | --- | --- |
| #8 | REQ-008 | 탐지 기능 | 동일 IP 기준 DDoS 공격 탐지 | 최근 n분 로그를 대상으로 동일 client_ip와 domain별 로그 수를 계산하고, 합산된 요청 수가 n회 이상이면 악성 IP로 판단해야 한다. | P0 | 중 | 1차 개발 핵심 기능 |
| #9 | REQ-009 | 예외 처리 | DDoS 탐지 예외 IP 적용 | 동일 IP 기준 DDoS 탐지 시 config에 등록된 예외 IP는 악성 IP 판단 대상에서 제외되어야 한다. | P0 | 하 | REQ-008과 함께 구현 |

## 공통 의존 요구사항

- REQ-002: 탐지 시간 범위, 요청 수 기준, 예외 IP 목록은 config에서 관리한다.
- REQ-004: 최근 n분 로그를 Elasticsearch ESQL API로 조회한다.
- REQ-005: `timestamp`, `client_ip`, `domain`, `path`, `protocol_status`를 내부 분석 구조로 파싱한다.
- REQ-018: 알림 본문에 탐지 유형, 대상 IP/domain, 발생 횟수, 탐지 기준, 탐지 사유가 전달될 수 있어야 한다.
- REQ-026: 탐지 결과 생성과 실패 상황을 로그로 확인할 수 있어야 한다.
- REQ-028: 정상 탐지, 미탐지, 예외 IP 케이스를 단위 테스트로 검증한다.
